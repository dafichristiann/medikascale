import { useEffect,useState } from 'react'; import type { RiwayatArsip } from '@/types'; import { workflowApi } from '@/api/workflows'; import { Card,PageTitle,StatusBadge } from './ClinicalUi';
export default function ArsipHistoryPage(){const[items,setItems]=useState<RiwayatArsip[]>([]);useEffect(()=>{workflowApi.riwayat().then(setItems)},[]);return <div><PageTitle title="Riwayat peminjaman" description="Jejak transaksi arsip bersifat permanen dan tidak dapat dihapus."/><Card><div className="overflow-x-auto"><table className="w-full text-[13px]"><thead className="text-left text-slate text-[11px] uppercase"><tr><th className="p-3">Waktu</th><th>Pasien</th><th>Tindakan</th><th>Keterangan</th><th>Petugas</th></tr></thead><tbody>{items.map(x=><tr key={x.id} className="border-t border-border"><td className="p-3 whitespace-nowrap">{x.waktu}</td><td className="font-semibold">{x.nama_pasien}<div className="font-mono text-xs text-slate">{x.no_rm}</div></td><td><StatusBadge status={x.tindakan}/></td><td>{x.keterangan}</td><td>{x.petugas}</td></tr>)}</tbody></table></div></Card></div>}
